{
  "name": "@bots/migue-ambiente",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Bot Migue Ambiente y worker de ingesta. Dos puntos de entrada, misma logica compartida en @migue/dominio.",
  "main": "src/index.ts",
  "scripts": {
    "start": "node --env-file=.env src/index.ts",
    "dev": "node --env-file=.env --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "start:worker": "node --env-file=.env src/worker/index.ts",
    "dev:worker": "node --env-file=.env --watch src/worker/index.ts"
  },
  "dependencies": {
    "@bots/core": "workspace:*",
    "@migue/dominio": "workspace:*",
    "grammy": "^1.32.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2"
  }
}
